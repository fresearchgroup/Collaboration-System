

[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/CollaborationSystem
ExecStart= gunicorn --workers 3 --bind localhost:8000 CollaborationSystem.wsgi:application

[Install]
WantedBy=multi-user.target

